===================================
Bugs:

- buildComp:

	5. Tlw. falsche Formatierung -> Input muss UTF-8 kodiert sein. Hier nochmal nachschauen.

	WICHTIG aber EINFACH
!	1. Bei leeren CSV-Datenfeldern wird in der Ressource "NaN" eingetragen. 
        Siehe 2. -> Überarbeitung des Pfad Auslesens, darauf basierend dann dem Nutzer beim Mapping anzeigen, was Pflicht ist und welches Format der Input haben muss. 

	4. Sprechende Fehlermeldung bei Missing <<index>>-Angaben im Mapping!

- pathExport:

	WICHTIG aber AUFWÄNDIGER
	2. Bei Pflichtpfaden dies mitschleppen und in GUI/beim Mapping deutlich machen
		- Übersicht über alle PFLICHT-PFade -> Diese Daten müssen dann auch in Quelldaten vorhanden sein und gemappt werden
		- Datentyp UND Pflichtfeld-Wert des Felds mit dem Pfad speichern und mitschleppen (oder aus WebTemplate zu einer id abrufen?)
			- Aufbau eines schönen Pfad Dictionaries mit 
				- Pfad
				- Pflicht-Bool
				- Datentyp

	5. DV_Quantity hat auch (wie DV_ORDINAL und DV_CODED_TEXT) eine Liste von möglichen Werten. Aber nur einen Suffix -> Daher für Pfade wohl egal, oder?

!	SUPER WICHTIG TODO
!	3. Suffixe können in Listen stehen (Bsp. DV_Ordinal Sturz im NATARS_Tzusatz.opt) -> Hierzu alle Suffixe/Pfade ermitteln und erzeugen

- handleOPT:
	
	2. Before uploading the opt check if it already exists. Server timeouts occur often at the moment..

- Aus dem WebTemplate lese ich keine Pfade wie die folgenden aus. Die stehen da auch nicht drin. Was bedeutet das für uns?
	_other_participation:0
	_uid
  Welche Pfade gibt es dort noch bzgl. Audit etc., brauchen wir die?

===================================
Neu:

KERNFUNKTION
- WebTemplate to Path-Dict (or Array of Dicts with Cardinality and Suffixes and DataType!)

- Mapping GUI mit 
	- Autovervollständigung
	- Übersicht, welche Pfade bereits gemappt wurde + verhindern, dass Pfade doppelt zugeordnet werden

===================================
WICHTIG
- Uploader (ehr_id|composition/daten) steht noch aus

NETT
- SQL-Connector für SQL-Abfrage bei DB als Input-Möglichkeit

===================================
Idee:
Erstellen eines Composition-Template mit Platzhalter für den Datenwert.
{
    "natars_tzusatz/body_mass_index/beliebiges_ereignis:0/body_mass_index|magnitude": {{magnitude_aus_daten}},
    "natars_tzusatz/body_mass_index/beliebiges_ereignis:0/body_mass_index|unit": {{unit_wert_string_aus_daten}}
}
==================================
About Web Templates
- simplified IM Simplified Data template (simSDT) which is based on the web template format created by Marand for the Better platform (https://ehrbase.readthedocs.io/_/downloads/en/latest/pdf/)